# FCV Data Analyst – API keys and options
# Copy this file to .env and edit with your values. Do not commit .env.
# Restart the backend after editing.

# Optional: which provider to use on startup (openai | anthropic | azure)
# LLM_PROVIDER=anthropic

# Optional: model to use when using env config (default: claude-opus-4-6)
# LLM_MODEL=claude-opus-4-6

# OpenAI (if provider is openai)
OPENAI_API_KEY=

# Anthropic / Claude (if provider is anthropic)
ANTHROPIC_API_KEY=

# Azure OpenAI endpoint (optional; only if provider is azure)
# AZURE_OPENAI_ENDPOINT=

# Optional: path to default ACLED CSV
# ACLED_DEFAULT_PATH=C:\Users\wb617270\OneDrive - WBG\FCV\acled_data\data\master\current\acled_data_current.csv
